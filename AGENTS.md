# AGENTS.md — Unity Editor Localization Core

## 適用範囲

本リポジトリの開発・保守に適用する。セッションに配布された共通規約と、Unityプロジェクト配下で作業する場合はプロジェクトの規約も参照する。既存の未コミット変更を保持する。

## ブランチ作成とバージョン更新

- 新しい開発ブランチを作成したタイミングで、`package.json` の `version` をそのブランチで開発する予定のバージョンへbumpする。リリース直前まで更新を延期しない。
- `vX.Y.Z` 形式のブランチでは、先頭の `v` を除いた値を設定する。例: `v1.0.1` ブランチを作成したら `version` は `1.0.1` にする。
- 対象バージョンがユーザー指示・合意済みの作業方針から確定できない場合は、版数を推測せず確認する。
- ブランチ作成後に、現在のブランチ名と `package.json` のバージョンの対応を確認する。

## リリース

- リリースCIは `package.json` のバージョンを基準に `vX.Y.Z` タグを作成し、CI実行対象コミットへ付与する。GitHub Releaseも同じタグを使用する。
- 既存のリリースタグは上書き・付け替えをしない。
- ブランチ作成・バージョン更新と、CI実行・リリース公開は別の操作として扱い、それぞれの許可範囲に従う。
- 利用者向けのリリース手順は `README.md` に記載する。

## 作業記録

- 方針・運用の正本はリポジトリ直下の `ORCHESTRATION.md`、進捗の正本は `CHECKPOINT.md` とする。
- 変更時は関連文書と作業記録も更新する。上記2ファイルはローカル専用としてGit除外を維持し、コミット対象に含めない。
